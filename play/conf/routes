# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                       Application.index
GET     /albums/{genre}				            Application.list
GET	    /xml/albums/{genre}			            Application.listXml(format:'xml')
GET	    /xml/albums     			            Application.listXml(format:'xml')
POST    /albums                                 Application.save
GET     /albums                                 Application.list
GET     /albums/{id}                            Application.form
GET     /album/new                              Application.form
POST	/xml/album/new		                    Application.saveXml

# Map static resources from the /app/public folder to the /public path
GET     /public/                                staticDir:public

# Catch all
*       /{controller}/{action}                  {controller}.{action}
